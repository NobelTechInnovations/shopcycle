<span class="price price--modern">
  {% if variant.comparePrice and variant.comparePrice > variant.price %}
    <s class="price__compare">{{ variant.comparePrice | money }}</s>
  {% endif %}
  <span class="price__current">{{ variant.price | money }}</span>
</span>
